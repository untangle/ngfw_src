#! /bin/bash

# 
# This script starts a tunnel VPN
#

tunnel_id=${1}
conf_dir=${2}
tunnel_name="tunnel-${tunnel_id}"

mkdir -p /run/openvpn

echo "Launching openvpn..."
/usr/sbin/openvpn --daemon ovpn-${tunnel_name} --cd ${conf_dir} --config ${conf_dir}/tunnel.conf </dev/null &

