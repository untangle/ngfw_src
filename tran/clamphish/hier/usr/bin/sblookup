#!/usr/bin/ruby

require 'base64'
require 'bdb'
require 'digest/md5'
require 'net/http'

db = BDB::Hash.open('hippieshit', nil, 'r');

ARGV.each do |host|
  hash = Digest::MD5.hexdigest("oU3q.72p#{host}")
end
