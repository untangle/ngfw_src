#! /bin/dash

url="http://localhost/setup/welcome.do\?console=1"
wh=$(xdpyinfo | awk '/dimensions:/ { gsub(/x/, ",", $2) ; print $2 }')

if [ -f /usr/bin/chromium ] ; then
  /usr/bin/chromium --window-size=$wh $url
else
  /usr/bin/iceweasel -width ${wh/,*} -height ${wh/*,} $url
fi
