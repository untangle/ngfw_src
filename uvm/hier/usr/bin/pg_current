#!/bin/sh
/usr/bin/psql -U postgres uvm -c'SELECT datname as db,usename as user,procpid as pid,waiting as wait,round(extract('epoch' from (now() - query_start))) as sec,current_query FROM pg_stat_activity'