<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE log4j:configuration SYSTEM "log4j.dtd">

<!-- ===================================================================== -->
<!--                                                                       -->
<!--  Log4j Configuration for UVM                                         -->
<!--                                                                       -->
<!--     Configuration of all uvm loggers lives here.                     -->
<!--                                                                       -->
<!-- ===================================================================== -->

<!-- $Id$ -->

<log4j:configuration xmlns:log4j="http://jakarta.apache.org/log4j/"
                     debug="false">

  <!-- ============================================== -->
  <!-- Appenders                                      -->
  <!-- ============================================== -->

  <appender name="UVMLog" class="org.apache.log4j.RollingFileAppender">
    <param name="File" value="${bunnicula.home}/log/uvm.log"/>
    <param name="Append" value="true"/>
    <param name="MaxFileSize" value="500KB"/>
    <param name="MaxBackupIndex" value="9"/>
    <param name="Threshold" value="ALL"/>
    <layout class="com.untangle.uvm.logging.MvPatternLayout">
      <!-- This is actually the default conversion, so we could leave it out... -->
      <param name="ConversionPattern" value="%D %-5p [%c{1}] %m%n"/>
    </layout>
  </appender>

  <!-- Log Emailer -->

  <appender name="EmailAppender"
            class="com.untangle.uvm.logging.SmtpAppender">
    <!-- We use the default conversion pattern, so it's omitted. -->
  </appender>

  <!-- ============================================== -->
  <!-- Categories                                     -->
  <!-- ============================================== -->

  <category name="com.mchange">
    <priority value="INFO"/>
  </category>

  <category name="org.apache.catalina">
    <priority value="WARN"/>
  </category>

  <category name="org.hibernate">
    <priority value="WARN"/>
  </category>

  <category name="com.untangle.uvm.argon">
    <priority value="WARN"/>
  </category>

  <category name="com.untangle.jnetcap">
    <priority value="WARN"/>
  </category>

  <category name="com.untangle.jvector">
    <priority value="WARN"/>
  </category>

  <!-- normal level for engine is INFO -->
  <category name="com.untangle.uvm.engine">
    <priority value="INFO"/>
  </category>

  <category name="com.untangle.uvm.logging">
    <priority value="WARN"/>
  </category>

  <category name="com.untangle.uvm.policy">
    <priority value="WARN"/>
  </category>

  <category name="com.untangle.node.util">
    <priority value="INFO"/>
  </category>

  <category name="com.untangle.uvm.reporting">
    <priority value="DEBUG"/>
  </category>

  <category name="com.untangle.node.portal.proxy">
    <priority value="INFO"/>
  </category>

  <!-- ============================================== -->
  <!-- The Root Category                              -->
  <!-- ============================================== -->

  <!-- All UVM logs go to the UVMLog file -->
  <root>
    <priority value="WARN"/>
    <appender-ref ref="UVMLog"/>
    <appender-ref ref="EmailAppender"/>
  </root>
</log4j:configuration>
