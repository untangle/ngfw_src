#!/bin/dash

SIGNATURE_DIRECTORY=$1

## Helper script so that all of the last updates have a common place to call.  
if [ -n "${SIGNATURE_DIRECTORY}" ]; then
    ## Ignore errors
    find "${SIGNATURE_DIRECTORY}" -type f -printf "%T@\n" 2>/dev/null | sed -e 's/\..*$//' | sort -n | tail -n 1
fi
